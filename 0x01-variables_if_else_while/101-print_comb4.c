#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: (0) Success
 */

int main(void)
{
	int a, b, c;

	for (a = '0'; a < '9'; a++)
	{
		for (b = a + 1; b <= '9'; b++)
		{
			if ((b != a) != c)
			{
				putchar(a);
				putchar(b);
				(putchar(c);
				 if (a == '8' && b == '9'
					 continue;
					 putchar(',);
					 putchar(' ');
			}
		}
	}
	putchar('\n');
	return(0);
}
